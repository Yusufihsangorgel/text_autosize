name: text_autosize
description: >-
  Automatically resize text to fit its bounds. A maintained, TextScaler-aware
  successor to the auto_size_text API.
version: 0.1.0
repository: https://github.com/Yusufihsangorgel/text_autosize
topics:
  - text
  - autosize
  - typography
  - responsive
  - widgets

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
